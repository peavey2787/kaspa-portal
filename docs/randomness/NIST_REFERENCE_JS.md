# NIST SP 800-22 JavaScript reference

These JavaScript files are retained reference/QA code, not runtime SDK code. NIST statistical tests are health diagnostics; passing them is not proof of cryptographic unpredictability.
